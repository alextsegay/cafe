# Expo SDK 54

This project targets **Expo SDK 54**. Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code, and prefer
`npx expo install` to add packages so versions match the SDK.
