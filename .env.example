# Database
# IMPORTANT: Change these credentials before production deployment
DATABASE_URL="mysql://cafe_app:your_strong_password@localhost:3306/cafe_db"

# Authentication
# IMPORTANT: Generate a strong random JWT secret for production
# Use: openssl rand -base64 32
JWT_SECRET="generate-a-strong-random-secret-here"

# App URL (for QR code generation)
# Change to your production domain
NEXT_PUBLIC_APP_URL="http://localhost:3000"
